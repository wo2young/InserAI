server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.1.163:5432/insurai   # 상대방 DB 서버 IP
    username: postgres                             # 상대방 DB 계정
    password: tmshdlwls12

  jpa:
    hibernate:
      ddl-auto: validate   # ⚠️ 협업 시 update도 위험 → validate 권장
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
